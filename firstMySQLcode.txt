REATE TABLE users (
    user_id VARCHAR(50) PRIMARY KEY,       -- Kullanıcının kendi belirlediği eşsiz ID
    email VARCHAR(120) UNIQUE NOT NULL,    -- Kullanıcının e-posta adresi
    password_hash VARCHAR(256) NOT NULL,   -- Hashlenmiş şifre
    
    quiz1_score FLOAT DEFAULT 0,
    quiz1_time_spent INT DEFAULT 0,
    quiz2_score FLOAT DEFAULT 0,
    quiz2_time_spent INT DEFAULT 0,
    quiz3_score FLOAT DEFAULT 0,
    quiz3_time_spent INT DEFAULT 0,
    quiz4_score FLOAT DEFAULT 0,
    quiz4_time_spent INT DEFAULT 0,
    quiz5_score FLOAT DEFAULT 0,
    quiz5_time_spent INT DEFAULT 0,
    quiz6_score FLOAT DEFAULT 0,
    quiz6_time_spent INT DEFAULT 0,
    quiz7_score FLOAT DEFAULT 0,
    quiz7_time_spent INT DEFAULT 0,
    quiz8_score FLOAT DEFAULT 0,
    quiz8_time_spent INT DEFAULT 0,
    quiz9_score FLOAT DEFAULT 0,
    quiz9_time_spent INT DEFAULT 0,
    quiz10_score FLOAT DEFAULT 0,
    quiz10_time_spent INT DEFAULT 0,

    total_score FLOAT DEFAULT 0,           -- 10 quiz'in ortalama başarı oranı
    total_time_spent INT DEFAULT 0         -- 10 quiz için toplam çözme süresi
);
